name = "chalet-sync"
main = "worker.js"
compatibility_date = "2024-09-01"

# Les variables (AIRTABLE_BASE_ID, AIRTABLE_TABLE_ID, AIRTABLE_PHOTOS_TABLE_ID,
# AIRTABLE_PHOTOS_FIELD_ID) et le secret (AIRTABLE_PAT) sont configurés
# directement dans le dashboard Cloudflare — ils ne sont pas listés ici
# pour éviter de les versionner dans git.
